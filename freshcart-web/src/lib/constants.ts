// Re-exported from @freshcart/types, which is also imported by freshcart-backend, so
// checkout (which charges it) and the backend (which now charges it too) can't drift
// out of sync again.
export { FREE_DELIVERY_THRESHOLD, DELIVERY_FEE } from '@freshcart/types';
