// Shared between checkout (which charges it) and the product page (which advertises
// it) so the two can't silently drift out of sync again.
export const FREE_DELIVERY_THRESHOLD = 299;
export const DELIVERY_FEE = 40;
